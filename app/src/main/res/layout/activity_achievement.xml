<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.caitlin.crealchemy.AchievementActivity"
    android:background="@color/background"
    android:id="@+id/relativeLayout">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:id="@+id/imageViewTitle"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/achievements"
        android:contentDescription="@string/content_description_achievements"
        android:layout_marginTop="-30dp" />

    <GridView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/gridViewAchievements"
        android:layout_below="@+id/imageViewTitle"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="-20dp"
        android:numColumns="3"
        android:background="@color/buttonBackground" />

    <ImageView
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:id="@+id/imageViewIcon"
        android:layout_alignTop="@+id/gridViewAchievements"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/content_description_achievement" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Large Text"
        android:id="@+id/textViewText"
        android:layout_below="@+id/imageViewIcon"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
